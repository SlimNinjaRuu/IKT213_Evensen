# IKT213 – Assignment 3 (Edges, Resizing, Template Matching)

This project implements the required image-processing functions in **OpenCV**:
- `sobel_edge_detection(image)`
- `canny_edge_detection(image, threshold_1, threshold_2)`
- `template_match(image, template, threshold=0.9)`
- `resize(image, scale_factor: int, up_or_down: str)`

All outputs are saved to `assignment_3/outputs/`.

---
